# DevMate AI 协作约束

本文件适用于仓库内所有人工和 AI 开发。

## 开发前

- 阅读 `docs/ENGINEERING_STANDARDS.md` 和相关模块文档。
- 保持模块化单体，不新增微服务、MQ、Redis 或向量数据库，除非当前需求有明确依据。
- 先确认输入、输出、状态变化、失败路径和测试方案，再修改代码。

## 必须遵守

- Controller 只处理协议转换和校验，不能直接访问 Mapper。
- Entity 不直接作为接口请求或响应；使用 DTO/Response。
- 使用构造器注入，禁止字段注入。
- 数据库事务只包围数据库状态变化；网络、Git、文件扫描和模型调用不放进长事务。
- 所有外部调用必须有超时、可读错误和失败状态。
- 禁止记录或提交密码、Token、完整私有源码和敏感 Prompt。
- 模型不能直接执行任意 Shell、SQL 或访问数据库，只能调用受控 Tool。
- BIGINT ID 对前端使用字符串。
- 已执行的 Flyway 迁移不可修改，只能新增版本。
- 不覆盖用户无关修改，不使用破坏性 Git 命令。

## 完成标准

- 正常、边界和失败路径有测试。
- 后端运行 `./mvnw test`；前端变更运行 `npm test -- --run` 和 `npm run build`。
- 运行 `git diff --check`，确认没有密钥、构建产物或工作区源码进入 Git。
- 业务、架构或开发顺序变化同步更新 `docs/PROJECT_LOG.md` 和对应设计文档。
- 只把已经实现、验证并能解释的能力写入 README 或简历材料。
