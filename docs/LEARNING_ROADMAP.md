# 面试导向学习与开发路线

## 1. 目标

本路线服务于 Java 后端校招/实习面试。目标不是尽快堆完所有框架，而是完成一个可运行、可演示、可测试并能独立讲解的 Java 智能代码审查 Agent。

建议按照 10 个里程碑推进。以每天 2–3 小时计算，可以按 10 周安排；实际进度以阶段验收为准，不为赶时间跳过理解和测试。

每个阶段结束时必须做到：

1. 能画出请求、数据和异常流转。
2. 能解释核心代码和技术取舍。
3. 能不依赖 AI 修改一个小需求。
4. 能通过日志或断点定位一次故障。
5. 有自动化测试或可重复验收步骤。

## 2. 每次学习与开发节奏

一次 2–3 小时的开发建议分为：

| 环节 | 建议时间 | 产出 |
|---|---:|---|
| 学习概念 | 30–40 分钟 | 能用自己的话解释原理 |
| 设计功能 | 20 分钟 | 输入、输出、约束、失败路径 |
| 编写代码 | 60–80 分钟 | 一个小而完整的功能 |
| 测试调试 | 30–40 分钟 | 正常、边界和异常用例 |
| 复盘记录 | 15 分钟 | 文档、Git 提交、面试问答 |

不要连续生成大量代码后再统一理解。每次只完成一个可以运行和验证的小闭环。

## 3. 里程碑路线

### 里程碑 1：Spring Boot 项目管理闭环（已完成）

开发内容：

- 完成项目创建、详情、分页、修改和逻辑删除。
- 完成 DTO、参数校验、统一异常和状态管理。
- 使用 MyBatis-Plus 操作 MySQL。
- 增加接口、Service 和数据库测试。
- 增加 OpenAPI 接口文档。

必须掌握：

- HTTP 方法和常见状态码。
- Controller、Service、Mapper 的职责。
- IOC、依赖注入和 Spring Bean。
- AOP 代理和 `@Transactional` 事务边界。
- 主键、外键、索引、逻辑删除和分页。
- DTO 为什么不直接复用数据库实体。

AI 可以协助：

- 生成 DTO、字段映射和测试初稿。
- 检查参数边界和重复代码。
- 解释日志和 SQL。

开发者必须主导：

- `project` 表和接口契约。
- 事务范围、状态变化和异常语义。
- 测试场景及最终代码审查。

面试检查题：

- 一次创建项目请求经过哪些组件？
- `@Transactional` 在什么时候提交和回滚？
- 为什么删除项目使用逻辑删除？
- 为什么不能让客户端传入 `status` 和 `deleted`？

验收：能通过 API 完整管理项目，并在 MySQL 中验证结果。

### 里程碑 2：源码导入与 Java 结构解析（已完成）

开发内容：

- 支持 Git 仓库、本地目录或压缩包的统一源码来源接口。
- 安全扫描 Java 文件，解析包、类、方法、注解和行号。
- 保存 `knowledge_document`、`knowledge_chunk` 和 `index_task`。
- 使用文件哈希识别重复或变更内容。

必须掌握：

- Java 文件 IO、NIO、流和资源关闭。
- 路径穿越、压缩炸弹、文件数量和大小限制。
- Git 仓库、分支、Commit 和工作树。
- AST 与字符串切割的区别。
- 数据库父子关系和任务状态机。

AI 可以协助：

- 生成文件遍历、哈希和映射代码初稿。
- 提供恶意路径和异常文件测试样例。

开发者必须主导：

- 文件安全边界、允许范围和失败处理。
- Java 代码切片需要保留哪些结构信息。

面试检查题：

- 为什么不能直接读取用户传入的任意路径？
- 如何防止 Zip Slip？
- 为什么按类和方法切片比固定字符数更适合代码？

验收：导入示例项目后，可以查询文件、类、方法和源码行号。

### 里程碑 3：Git Diff、审查覆盖与精确定位（已完成）

开发内容：

- 读取两个 Git revision 之间的 Diff。
- 正确处理新增、修改、删除和 Rename。
- 把变更行映射到 Java 类、方法和注解。
- 生成完整审查、部分审查和跳过文件覆盖清单。
- 建立代码证据锚点，由服务端映射真实行号。

必须掌握：

- Git Diff、Hunk、base/head revision 和三点比较。
- 新增、删除、重命名文件的差异。
- 为什么模型不能自由生成行号。
- 为什么“没有发现问题”必须附带审查覆盖范围。

AI 可以协助：

- 生成 Diff 解析测试样例。
- 补充 Rename、空 Diff 和大 Diff 边界案例。

开发者必须主导：

- 审查范围、覆盖语义和行号可信边界。
- 确认每个变更文件都能解释其处理结果。

面试检查题：

- 为什么代码审查应该以 Diff 为入口？
- 大 PR 因 Token 被截断时为什么不能仍然返回“审查完成”？
- 如何防止删除文件和 Rename 导致解析异常？

验收：每个变更文件都有完整、部分或跳过状态，且真实行号由确定性代码映射。

实现复盘：

- JGit 的 `Edit` 同时提供基准版本 A 区间和目标版本 B 区间，不能只保存 B 区间，否则删除行永远无法定位。
- 目标版本符号复用导入阶段持久化的 `knowledge_chunk`；基准版本只为本次 Diff 从 Git Blob 读取并在内存解析，避免知识库无限复制历史源码。
- 映射证据带 `BASE/TARGET`，面试时要能解释删除行属于基准版本，而新增行属于目标版本。
- Rename 是相似度检测，不只是文件名变化；固定测试同时覆盖纯 Rename 和 Rename 后修改内容。

### 里程碑 4：静态分析与项目规则

开发内容：

- 接入至少一种 Java 静态分析能力。
- 将工具结果转换成统一 Finding。
- 按路径、注解和框架特征选择事务、缓存、MQ、SQL 等规则。
- 对结果进行去重、严重程度映射和位置验证。

必须掌握：

- 编译问题、静态规则问题和语义风险的区别。
- 为什么确定性问题不应该交给 LLM 猜。
- 规则优先级、项目规则和系统默认规则。

AI 可以协助：

- 接入工具和生成结果转换器初稿。
- 补充规则测试样例。

开发者必须主导：

- 规则取舍、严重程度、误报判断和证据标准。

验收：确定性缺陷能稳定返回文件、行号、规则和严重程度。

当前已经掌握的实现：

- PMD 通过 Java API 在应用内运行，规则集由 DevMate 固定管理，不调用目标项目的 Maven/Gradle。
- 分析任务采用“准备短事务 → 工具执行 → 完成/失败短事务”，避免耗时分析占用数据库事务。
- Finding 通过规则、路径、行号和消息生成指纹，数据库唯一键提供最终去重保护。
- 静态问题必须直接命中目标版本 Diff 行；调用链和项目上下文相关问题留给下一阶段规则与 RAG。
- JDK AST 已提取调用表达式、配置引用和数据访问入口；只有同类唯一目标才建立确定 `target_chunk_id`。
- “调用事实”和“真实动态目标”必须区分，无法唯一解析时保留空目标而不是猜测。
- `@Transactional` 自调用规则依赖唯一解析的同类目标；循环查询和锁内 IO 规则只给出风险场景与验证建议。
- PMD 放在限时虚拟线程，数据库上下文在请求线程读取，避免依赖另一个线程不可见的未提交事务。
- 已将 YAML/Properties 展平为脱敏配置 Chunk，并把 `@Value`/`@ConfigurationProperties` 关联到候选定义。
- 面试时必须说明 Spring Profile 和环境变量会影响最终值，静态关联只提供证据候选，不宣称运行时唯一值。
- 已从受控迁移 SQL 提取表、列、索引和约束，并将实体注解关联到表定义；迁移事实不等于线上最终 Schema。

面试自测题：

- 为什么选择 PMD 而不是第一版就用 SpotBugs？
- PMD 在应用线程内执行时如何限制时间、文件数量和路径？
- 为什么不直接启用全部 PMD 规则？
- 如果 PMD 返回了旧代码中的问题，为什么当前提交不应该展示它？
- 任务失败后数据库中应该保留什么状态，为什么不能整段流程放进一个事务？
- 为什么方法名和参数数量仍不足以可靠解析所有 Java 调用？
- 为什么 Mapper 命名只能作为数据访问线索，不能直接判定 N+1 或高并发缺陷？
- Spring 默认代理模式下，为什么同一个 Bean 内部调用可能绕过 `@Transactional`？
- 数据库调用为什么不应该放在 synchronized 锁的范围内？
- 为什么知识库不能保存配置密码、令牌和连接串原文？
- 为什么同一配置键可以解析出多个候选目标？
- 为什么索引定义存在仍不能证明某条查询一定走索引？
- 为什么代码审查系统不应该直接连接生产数据库读取结构？

### 里程碑 5：面向变更的 RAG 与检索评测

开发内容：

- 对类、方法和文档生成 Embedding。
- 实现项目、版本和符号过滤。
- 混合检索完整方法、调用链、配置、SQL 和测试。
- 实现去重、重排、Token 预算和裁剪记录。
- 建立固定问题、期望文件和期望方法评测集。

必须掌握：

- Embedding、余弦相似度、Top-K 和元数据过滤。
- Chunk 边界、上下文完整性和版本一致性。
- Recall@K、Precision@K 与 Token 预算。
- 为什么 Diff 限定范围但不能替代完整文件上下文。

AI 可以协助：

- 接入模型 SDK 和向量存储适配层。
- 生成评测脚本和候选问题。

开发者必须主导：

- Chunk 策略、正确答案标注和失败案例分析。

面试检查题：

- RAG 如何为一个变更方法找到必要上下文？
- 为什么只给模型 Diff Hunk 容易产生误报？
- 如何记录由于 Token 预算被裁剪的上下文？

验收：变更相关代码稳定出现在前 K 个结果中，并能量化检索表现和裁剪范围。

当前已经掌握的实现：

- 使用 `project_id + revision` 隔离候选、关系和种子，外部 Chunk ID 不能跨项目复用。
- 以 Diff 目标符号作为确定性种子，再通过 `code_reference` 扩展调用、配置和数据库关系。
- 用符号、路径、内容词和逆文档频率建立 `lexical-graph-v1` 可解释基线。
- 通过内容哈希去重，并明确记录 Top-K、Token 预算和重复内容造成的裁剪。
- 候选达到扫描上限时显式返回不完整标记，避免把局部结果包装成完整召回。
- 将固定问题、预期文件/符号和 K 持久化，运行结果绑定 revision 与检索配置版本。
- 使用 Recall@K、Precision@K、HitRate@K 和 MRR 衡量召回与排序；未索引预期项不伪装成算法失败。
- 当前 Token 数是近似估算，接入具体模型后必须使用对应 tokenizer。

下一步要掌握：

- Embedding 维度、归一化、余弦相似度与批量请求；
- 向量存储适配层、幂等 `vector_id` 和版本过滤；
- 关键词、向量、关系图三路结果的融合和重排；
- 用同一评测集分析中文语义、同义词和跨文件检索的增益与失败案例。

当前已经完成的向量实现：

- `EmbeddingProvider` 隔离本地测试算法与 DashScope 真实语义模型，密钥只从环境变量读取。
- 使用项目、revision、provider、模型、维度和内容哈希构造索引版本，避免旧代码向量污染新提交。
- 远端 Embedding 在事务外分批执行，结果以短事务保存；失败后重复执行会跳过成功批次。
- `LEXICAL/VECTOR/HYBRID` 共用同一预算与返回协议，Hybrid 使用 RRF 融合不同量纲的排序。
- 请求向量模式但索引不可用时显式返回 `LEXICAL_FALLBACK`，评测不会把降级结果伪装为 Hybrid。
- MySQL 当前只承担小规模线性向量检索，能够讲清楚从适配层演进到 ANN 向量库的触发条件。

新增面试自测题：

- 为什么本地哈希向量适合测试闭环，却不能证明语义召回能力？
- 为什么向量索引需要绑定 provider、模型和维度？
- RRF 为什么不直接相加关键词分数和余弦相似度？
- 为什么 Embedding HTTP 请求不能处于数据库事务中？
- 如何保证批量向量化失败后可以安全重试？
- 如何用同一评测集判断 Hybrid 是否真的优于 Lexical？

### 里程碑 6：AI 代码审查 MVP

开发内容：

- 根据 Diff 检索调用链、配置、数据库和团队规范。
- 设计并发、事务、缓存、MQ、SQL、安全和架构审查规则。
- 让模型输出结构化 Finding。
- 每条结果包含证据、触发场景、建议和验证方法。

必须掌握：

- Prompt 的角色、约束、上下文和输出结构。
- Structured Output 和普通文本生成的区别。
- 置信度、事实、推断和待验证项的区别。
- 并发、事务、缓存和消息一致性的 Java 基础。

AI 可以协助：

- 生成 Prompt 候选版本和结构化输出模型。
- 作为第二视角提出潜在风险。

开发者必须主导：

- 审查规则、证据标准、误报判断和最终采纳。
- 亲自构造包含已知隐性风险的示例代码。

面试检查题：

- 如何证明模型发现的问题不是幻觉？
- 为什么每个问题都必须给出代码依据和验证方法？
- 如何审查事务失效、缓存一致性或重复消费问题？

验收：能够稳定识别一组预先设计的隐性风险案例。

当前已经掌握的实现：

- AI 审查使用独立任务，不复用静态分析状态；二者失败语义、成本和重试方式不同。
- 流程采用“准备短事务 → RAG/模型事务外执行 → 完成或失败短事务”，避免网络等待占用数据库连接和锁。
- `AiReviewModel` 隔离模型实现，DashScope Provider 只负责协议、超时、JSON 解析和脱敏异常。
- Prompt 把仓库内容标记为不可信数据，限定 JSON Schema、风险类别和结论语义。
- 服务端验证模型引用的 Chunk，只从数据库证据映射项目、revision、文件和行号。
- Structured Output 只解决格式概率，不替代枚举、范围、归属、重复和业务语义校验。
- 数据库唯一键承担并发幂等最终保护；超时任务转为失败后才允许重试。
- 审计只保存 Token、耗时、模型、Prompt 版本和请求哈希，不复制私有源码。

新增面试自测题：

- 为什么模型返回合法 JSON 仍然可能是不可信数据？
- 如何防止模型引用另一个项目的 Chunk？
- 为什么 `updateById(entity)` 不能可靠地把 nullable 幂等键清空？
- 模型超时、应用进程崩溃和数据库写入失败分别会留下什么状态？
- 为什么同步 MVP 可以接受，但高流量批量审查最终需要异步任务？

### 里程碑 7：Agent 与 Tool Calling

开发内容：

- 提供 `analyzeGitDiff`、`runStaticAnalysis`、`searchCode` 等工具。
- 允许 Agent 在受控范围内选择工具。
- 增加权限、参数校验、超时、最大步数和调用日志。

必须掌握：

- Agent 与普通 LLM 调用的区别。
- Tool 描述、参数 Schema、执行器和结果回传。
- 为什么大模型不能直接访问数据库或执行任意 Shell。
- 如何防止循环调用和越权。

AI 可以协助：

- 生成 Tool 适配代码和 Prompt 初稿。
- 补充异常、超时和错误参数测试。

开发者必须主导：

- 工具权限、安全边界、调用上限和失败降级。

验收：能够从日志还原 Agent 为什么选择某个工具以及工具返回了什么。

当前已经掌握的实现：

- Function Calling 是“模型返回工具名和 JSON 参数 → Java 执行 → 以 `tool_call_id` 回填结果 → 再调用模型”，不是模型直接运行 Java。
- `AgentToolRegistry` 决定唯一可调用白名单；Schema 用于提示模型，Java 校验才是可信边界。
- `projectId/revision/taskId` 来自服务端固定上下文，模型无法跨项目或切换版本。
- 使用单工具超时、输出上限、总步数与重复签名计数阻止卡死、循环和额度失控。
- `tool_call_log` 只保存参数哈希、键名和结果摘要，既能审计又不复制私有源码。
- 最终结构化审查复用 Chunk 白名单校验；Agent 没有成功检索代码时失败关闭。
- 保留固定流水线作为对照，阶段 8 用同一缺陷集比较质量、Token 与延迟，而不是主观宣称 Agent 更好。

新增面试自测题：

- 模型返回了 Schema 合法参数，为什么 Java 仍要重新校验？
- 未知 Tool、工具超时、重复调用和无证据停止分别如何落库？
- 为什么读取已有 Diff/静态结果比让 Agent 自动重跑更适合第一版？
- 如果将来增加修改代码 Tool，还需要哪些授权、审批和回滚机制？

### 里程碑 8：审查反馈与效果评测（进行中）

开发内容：

- 保存 Finding 的采纳、驳回、误报、延期和备注。
- 建立已知缺陷、标准答案与无缺陷对照样本。
- 使用同一 Diff 比较固定流水线与 Agent 的命中、漏报、误报、Token 和耗时。
- 分析无法自动匹配的 Finding，并由人工复核失败原因。

必须掌握：

- `PUT` 幂等语义、唯一键和项目归属校验。
- 反馈状态与标准答案的区别，特别是为什么 `REJECTED` 不等于误报。
- Precision、Recall、漏报、误报和无缺陷样本的意义。
- 为什么横向比较要固定模型配置、revision 和数据集，同时冻结并分别记录 FIXED/AGENT 各自的 Prompt 与检索版本。
- 为什么 V13 唯一键不包含 `review_task_id`，批量录入时必须复用同一个 Diff。
- 为什么外部 8 次 HTTP 写入不能用一个数据库长事务包装，以及全批预检、唯一键、回读和可恢复重跑各解决什么问题。
- 为什么 AI 创建请求必须绑定预期 Diff ID 与 revision，而不能让服务端在付费调用时临时选择“最近一次”任务。
- 为什么执行器零模型预检之后，服务端仍要在只读入口和创建任务事务内双重校验同一 Diff，以关闭两次操作之间的漂移窗口。
- 为什么响应丢失恢复必须排除请求前的旧 AI task ID；仅校验项目、Diff、revision 和模式仍可能把一次根本未到达服务端的请求误判为成功。

AI 可以协助：

- 生成反馈 API、Vue 交互和评测计算代码初稿。
- 补充边界测试和候选缺陷样本。

开发者必须主导：

- 定义标准答案、审查 Finding 匹配结果并解释失败案例。
- 决定哪些反馈能进入指标，防止用主观操作状态冒充事实标签。

验收：反馈可以可靠保存和恢复，并能用同一缺陷集生成可复现的两种审查路径对比报告。

当前已经掌握的实现：

- V12 对 `finding_id` 建唯一键，`PUT` 首次创建、再次提交更新同一资源。
- 服务端同时校验项目、Finding 和归属，避免通过 ID 越权写入其他项目。
- 即使认证已经完成也不接受客户端 `userId`；需要操作人时必须从服务端认证上下文获取，避免伪造身份。
- 最近审查响应批量回填反馈，避免 Finding 列表产生 N+1 查询。
- Vue 保存反馈只更新当前 Finding，不重新运行模型，因此不产生额外 Token 消耗。
- V13 使用数据集版本和内容哈希固定标准答案；同一 AI 任务重复评测不会重复创建运行。
- 固定流水线与 Agent 模式由任务创建时写入，不根据 Prompt 名称临时推断。
- Finding 只有在类别、文件路径一致且行区间重叠并形成唯一一对一候选时自动匹配；歧义项必须人工复核。
- 评测同时保存质量、Token、耗时和 Tool 成功率，避免只比较“看起来更聪明”的文本。
- 固定 fixture 通过契约测试不等于真实链路可用；还要逐层核对项目配置、导入任务、项目当前 revision、Diff base/target 和覆盖证据。
- 8 个真实源码导入和默认 `HEAD^ → HEAD` Diff 已完成，结果为 `8 PASS / 6 FULL / 2 PARTIAL`；标准答案缺陷行均有 candidate 的 `TARGET` 符号证据。
- 单场景重试只用于定位临时外部故障，最终结论必须来自完整数据集重跑。
- `--reuse-imports` 不会把失败任务当成功：它要求确定性项目已存在、最近导入为 `SUCCEEDED` 且任务/项目/candidate revision 一致，然后重新运行完整 Diff。
- `FULL/PARTIAL/SKIPPED` 描述证据覆盖程度；import 文件头未映射产生的 `PARTIAL` 不能被美化为 `FULL`。
- 后续通过精确 `FILE_HEADER/IMPORT` Chunk 补齐两侧证据，全新 H2 与全新 MySQL 真实复验均达到 `8 FULL / 0 PARTIAL`；解决方式是完善证据模型，而不是扩大类行号或修改状态计算。
- 隔离空库 MySQL 26.7 已完成 V1-V13 和同一 8 场景验收，证明 H2 通过之外，真实 MySQL 方言、迁移与持久化关系也能工作。
- 一次 GitHub 瞬时失败被保存为 `FAILED`，重试成功后再全量复核；可观察的失败记录比覆盖或删除失败更有利于恢复和排障。
- 标准答案创建已按 `projectId + reviewTaskId + newPath` 绑定目标 Diff，并要求标注范围、目标变更行和持久化 TARGET 符号形成三重交集。
- BASE 符号来自内存解析且没有 Chunk ID，不能证明目标 revision 已持久化；`PARTIAL` 文件只要缺陷行有 TARGET 证据仍可进入评测。
- MySQL 默认 `utf8mb4_unicode_ci` 不区分路径大小写，V14 因此保存 `new_path_hash` 并按任务建立索引；历史空哈希行使用 Java 精确比较兼容读取。TARGET 证据生成同样按 `knowledge_document.path_hash` 查询并复核完整路径，否则下游三重交集仍可能验证一条来自错误文件的 Chunk。
- manifest 同步会先验证全部 Import/Diff，再跨 Diff 查询同一项目和数据集的已有用例；任一字段漂移时整批零 POST，只补缺失项并在最后精确回读。
- POST 抛错不能直接判定“未提交”：服务端可能已提交但响应丢失，因此工具再次读取唯一键范围，区分真正失败和已提交恢复。
- 隔离 MySQL 首次同步为 `8 created / 8 verified`，立即重跑为 `0 created / 8 reused / 8 verified`；最终为 7 条 DEFECT、1 条 CLEAN，数据库已原地迁移到 V15。
- FIXED/AGENT 创建请求已携带预期 Diff ID、revision 和 UUID v4 attemptKey；服务端先做只读校验，并在创建 AI 任务与审计状态的事务内再次校验，避免预检通过后 Diff 发生变化。V15 唯一键用于响应丢失后的精确任务关联，不能用 latest 或盲目重发代替。
- 受控 A/B 执行器已完成零模型全批预检、逐场景 `FIXED → 评测 → AGENT → 评测`、失败即停和脱敏报告；预检中的任一 Diff、静态分析或标准答案漂移都会让整批保持零付费 POST。
- 模糊响应不会触发第二次付费 POST。执行器先保存请求前最近 AI task ID，随后只回收不同 ID 且全部身份字段一致的新任务；否则失败停止，不能用旧任务冒充恢复成功。
- 总体 Precision、Recall 和 F1 使用各场景 TP/FP/FN 求和后的微平均，并与 Token、耗时和 Tool 成功率分别聚合；这些计算与 canary/full 报告范围已通过 Mock/Fake 验证。

当前已完成与仍需亲自完成：

- 已完成的样本基础和真实 Diff：8 个 base/candidate 场景、确定性 Git revision、公开评测仓库、不泄漏答案的 case 分支，以及真实导入/Diff 证据报告。
- 已完成的数据库验收：隔离 MySQL 从空库迁移到 V13、原地升级 V14 和 V15，并持久化 8 个项目、8 个文档、46 个 Chunk、8 个成功导入和 16 个成功 Diff；V15 的 attemptKey 列与唯一索引已回读确认。
- 已补齐标准答案写入的项目归属、Diff 文件、目标变更行和 TARGET Chunk 证据约束，并完成正常、反向行范围、其他 Diff 文件、无变更交集、BASE-only、TARGET 不相交和跨项目测试。
- 已用 manifest 批量录入并回读 8 个 Diff，验证数量、字符串 ID、revision、类别、路径、行号、依据以及第二次零写入复用。
- 已完成受控 A/B 执行器、服务端预期 Diff 双重校验、零模型全批预检、精确响应恢复、失败即停和微平均聚合测试。
- 下一步必须先显式确认运行环境中的模型密钥和可用额度，再运行 1 个真实 canary；只有 canary 完整通过，才执行 8 组真实 FIXED/AGENT A/B。
- 解释 Precision/Recall/F1 的变化与具体失败案例，确认后才能写入简历。

本阶段新增必须掌握：

- Git commit SHA 由文件树、父提交、作者/提交者、时间和消息共同决定；只固定源码内容并不能保证 revision 可复现。
- 为什么评测仓库与标准答案清单必须分离，以及为什么分支名不能叫 `sql-n-plus-one` 等答案标签。
- 为什么每个场景使用独立分支和 `HEAD^ → HEAD`，而不是把多个缺陷和 CLEAN 变更合并成一个大提交。
- 如何用远端 branch HEAD、父提交和 revision 清单证明实际运行的是预期样本。

新增面试自测题：

- 固定随机种子为什么仍不足以保证 Git revision 一致？
- 如果模型能从分支名看到缺陷类别，评测结果为什么会失真？
- 为什么 CLEAN 对照不能与 DEFECT 样本位于同一个评测 Diff？
- 公开样本仓库发生强制推送后，系统如何通过 revision 清单发现数据集漂移？
- 为什么 import task、project currentRevision 和 Diff targetRevision 都必须等于 candidate SHA？
- 为什么 `case-005/008` 是 `PARTIAL`，为什么不能直接改成 `FULL`？
- 如何用 `FILE_HEADER/IMPORT` Chunk 补齐文件头覆盖，同时不污染类/方法语义检索？
- 为什么 TLS 单场景重试成功后仍要重新运行完整 8 场景？
- 为什么网络抖动时复核最近成功导入比再次克隆全部分支更稳定，又如何防止它掩盖失败？
- 为什么 H2 全量测试通过后仍要从空库执行真实 MySQL V1-V13，并检查最终业务表状态？
- 为什么失败导入记录应保留，而不是在重试成功后删除或覆盖？
- 为什么人工标准答案不能只校验“文件路径格式正确”，还必须绑定 Diff、目标变更行和 TARGET Chunk 证据？
- 为什么三重交集必须落在同一行，而不能分别找到“某条变更行”和“某个 TARGET 符号”就算通过？
- 为什么 `PARTIAL` 文件仍可能有可靠的缺陷行证据，这与最终准确率的适用范围有什么关系？
- 为什么不能直接依赖 MySQL 的 `new_path = ?` 判断 Git 路径相等，V14 为什么同时需要哈希索引和历史数据回退？
- 为什么 `(project_id, dataset_version, case_key)` 唯一键迫使批量工具复用原 Diff，而不能每次新建 Diff？
- 为什么预检可以保证“已知漂移时零写入”，却不能把 8 个 HTTP 请求变成一个原子数据库事务？
- POST 超时后如何判断服务端未提交，还是已经提交但响应丢失？
- 为什么补录完成后还要全量回读，而不能只相信 200 响应？
- 为什么执行器已经全批预检，服务端创建 AI 任务时仍必须再次核对预期 Diff ID/revision？
- 为什么恢复查询即使找到字段完全匹配的成功任务，也必须排除请求前记录的旧 task ID？

### 里程碑 9：增量索引与异步任务（进行中）

开发内容：

- 用真实 Embedding 输入指纹复用新 revision 中未变化的向量。
- 后续根据 Git Diff 复用未变文件的 Document/Chunk，并处理删除文件。
- 在同步任务耗时和失败数据证明需要后，再引入任务租约、RabbitMQ、重试和死信。

当前已经掌握的实现：

- 同 revision 的已存在向量属于幂等跳过；跨 revision 的相同输入属于增量复用，两者使用独立计数。
- Embedding 输入包含路径、Chunk 类型、符号和内容，因此只比较源码内容哈希会在移动文件或重命名时错误复用。
- V16 保存完整输入的 SHA-256，不保存额外源码；只从最近一次同模型配置成功 revision 读取复用候选。
- 新 revision 复制向量值但建立自己的 Chunk 绑定，检索仍按 revision 隔离，旧版本可用于复现历史审查。
- 远端 Provider 调用在事务外；每批新向量或复用绑定使用短事务更新向量、Chunk 和任务进度。
- 同 revision 重复导入不能删除重建 Chunk，因为向量外键会级联删除；现在采用零重写完成路径。
- 新 revision 按路径、类型和内容哈希复用未变文件的解析结果，但会为新 revision 建立独立 Chunk，并重新解析引用目标。
- 删除文件在新 revision 快照中缺席即失效，旧 revision 保留用于历史审查；物理清理由后续保留期策略决定。
- 导入任务使用单调时钟记录 clone、scan、plan、parse、persist 和 total；指标在成功、失败与同 revision 快速路径均可观察。
- 前端基础分析只编排确定性步骤，AI 模型调用继续显式触发；端到端测试中的 Fake 模型只能证明工程链路，不能证明模型质量。

必须亲自掌握：

- 为什么内容哈希、输入哈希和向量 ID 解决的是三个不同问题。
- 为什么增量复用不能破坏 project/revision/provider/model/dimensions 隔离。
- 为什么当前保留旧 revision，而不是看到“失效”就立即删除历史向量。
- 为什么复用旧文件的调用引用时不能复制旧 `targetChunkId`，而必须在新 revision 重新绑定。
- 为什么在没有耗时和失败数据前不应为了简历提前引入 RabbitMQ。

面试自测题：

- 一个方法内容没变但文件路径改变，能否复用向量，为什么？
- 跨 revision 复用为什么不能让新 Chunk 直接引用旧 revision 的向量记录？
- `processedChunks`、`reusedChunks` 和 `skippedChunks` 分别代表什么？
- 如果进程在一批向量保存后崩溃，为什么重跑不会再次调用已经保存的批次？
- 为什么同 revision 重复导入曾经会导致向量消失，外键级联在这里如何放大问题？
- 删除文件为什么不一定要立刻执行 DELETE，revision 快照和物理垃圾回收有什么区别？
- 什么运行数据能证明同步索引应该改成 MQ 异步任务？
- 为什么 Git revision 相同仍需要 `structure_version`？
- 为什么已有 Diff 或向量时不能原地重建同 revision Chunk？
- 项目行锁只放在准备短事务中，如何阻止导入与 Diff/向量任务之间的竞态？

### 里程碑 10：认证、安全、可观测性与部署

开发内容：

- Spring Security、JWT、项目成员和权限隔离。
- Redis 限流和短期会话。
- 模型耗时、Token、工具成功率和错误指标。
- Docker、Nginx、MySQL 和环境变量部署。

必须掌握：

- 认证与授权的区别。
- JWT 的生成、校验、过期和风险。
- Redis 常见数据结构、过期和缓存问题。
- 超时、重试、熔断和降级边界。
- 密钥管理和敏感日志脱敏。

AI 可以协助：

- 生成配置、部署文件和监控面板初稿。
- 检查常见安全配置问题。

开发者必须主导：

- 权限模型、数据隔离、限流值和故障降级方案。

验收：无权限用户不能访问其他项目，模型异常不影响普通业务接口。

当前完成：Spring Security、BCrypt、JWT、前端会话和项目成员隔离已形成第一版闭环。学习时优先掌握认证/授权、401/403、Filter/Service 双层校验、JWT 撤销限制和项目 OWNER 事务；Redis 限流与部署不能作为已实现能力。

### 里程碑 11：简历、演示与模拟面试

开发内容：

- 准备一个固定 Java 项目和多类风险提交。
- 演示导入、检索、审查、反馈和增量更新。
- 整理架构图、测试结果、失败案例和技术取舍。
- 根据实际代码生成最终简历项目经历。

必须掌握：

- 三分钟介绍完整项目。
- 从 HTTP 请求讲到数据库、RAG、Agent 和审查结果。
- 解释最困难的问题、失败方案和改进过程。
- 能现场修改小需求或定位一次异常。

验收：简历每一条描述都有对应源码、测试、运行记录和面试答案。

## 4. 当前阶段开发安排

### 第一次：审查反馈闭环（已完成）

- 完成 V12、幂等反馈接口、项目归属校验和 Vue 局部更新。
- 完成后端 96 项、前端 24 项测试、生产构建与真实 MySQL 迁移验收。

### 第二次：固定缺陷集、评测模型与真实 Diff 验收（已完成）

- 定义缺陷样本、无缺陷对照、标准答案和可解释的 Finding 匹配规则。
- 设计评测用例与运行表，先覆盖状态变化、重复运行和失败路径。
- 不把 Mock 结果或开发者 `REJECTED` 操作直接写成准确率。
- 发布确定性 case 分支，并完成 8 个真实导入和 Diff 的 revision、变更行与覆盖证据核对。

### 第三次：人工标准答案与 FIXED/AGENT A/B（进行中）

- 已强化标准答案创建服务，拒绝错误文件、反向行范围、未命中目标变更和缺少持久化 TARGET 证据的 DEFECT 用例。
- 已把 manifest 人工标准答案批量录入对应的 8 个成功 Diff，并完成首次创建、立即重跑零新增和全字段回读复核。
- 已完成受控 A/B 执行器、预期 Diff 请求绑定与服务端双重校验、零模型全批预检、漂移拒绝、响应丢失精确恢复、失败即停和微平均聚合测试。
- 下一步仅在显式确认环境模型密钥和额度后运行 1 个真实 canary；canary 通过后才执行 8 组真实 FIXED/AGENT A/B。
- 展示命中、漏报、误报、Token、延迟、工具成功率及失败案例。
- 评测结果稳定后再决定是否进入增量索引与异步任务阶段。

## 5. AI 使用红线

以下工作不能只依赖 AI 给出结论：

- 数据库关系、事务边界和状态机。
- 权限、并发、缓存、MQ 一致性和安全方案。
- RAG Chunk 策略和评测结果。
- Agent 工具权限和错误处理。
- 测试验收、故障定位和简历表述。

AI 生成代码后，开发者至少要能完成：

1. 指出入口类和核心方法。
2. 解释一条正常路径和一条失败路径。
3. 修改一个字段或业务规则。
4. 写出或补充一个测试。
5. 根据日志定位数据库或接口错误。

做不到以上五项时，该功能暂时不能写进简历。
